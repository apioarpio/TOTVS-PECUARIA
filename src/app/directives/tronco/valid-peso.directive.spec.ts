import { ValidPesoDirective } from './valid-peso.directive';

describe('ValidPesoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidPesoDirective();
    expect(directive).toBeTruthy();
  });
});
